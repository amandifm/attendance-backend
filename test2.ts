import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const start = new Date('2026-06-10T00:00:00Z'); const end = new Date('2026-06-10T23:59:59Z'); const attendance = await prisma.attendanceRecord.findMany({ where: { date: { gte: start, lte: end } }, include: { breaks: true } }); console.log('Attendance:', JSON.stringify(attendance, null, 2)); const shifts = await prisma.shift.findMany({ where: { date: start } }); console.log('Shifts:', JSON.stringify(shifts, null, 2)); } main().finally(() => prisma.$disconnect());
