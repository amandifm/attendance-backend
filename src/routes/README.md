# Route Modules

Routes are grouped by the same modules currently present in the mobile app. Keep new production features behind these module boundaries unless the PDF roadmap adds a clearly separate domain.
